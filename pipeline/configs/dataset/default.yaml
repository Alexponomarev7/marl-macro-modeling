defaults:
  - envs:
      - rbc
      - ncg
      - rbc_economy_with_policy
      - rbc_priority_based_weighted_contract
  - _self_

workdir: ${metadata.workdir}/dataset
enabled: True
train:
  type: dynare
  dynare_output_path: ???
  # Optional Dynare model-name lists, e.g. leave-one-environment-out:
  #   dataset.train.exclude_models=[Hansen_1985] dataset.val.include_models=[Hansen_1985]
  include_models: null
  exclude_models: null
val:
  type: dynare
  dynare_output_path: ???
  include_models: null
  exclude_models: null
  # type: envs
  # envs:
  #   - env_name: rbc
  #     num_steps: 1000
  #     num_combinations: 20
  #   - env_name: ncg
  #     num_steps: 1000
  #     num_combinations: 20
test:
  type: envs
  envs:
    - env_name: rbc
      num_steps: 50
      num_combinations: 20
    # - env_name: ncg
    #   num_steps: 1000
    #   num_combinations: 20
